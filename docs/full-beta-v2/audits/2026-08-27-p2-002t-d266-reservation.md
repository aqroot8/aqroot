# FBV2-P2-002T — D-266 scarce-pad reservation

**Date:** 2026-08-27 · **Task:** FBV2-P2-002T · **Starting HEAD:** `f5cd09c`
**Verdict:** section 22 decisive local gate **FAIL** — on the trunk alone.
Everything above the trunk passes, including **U18 8 of 8 with `U18.8` and `U18.9`
among them**, which has never happened before. No authoritative signal copper, no
placement ECO, no Q3 POFV written; the authoritative PCB is unchanged.

---

## 1. D-266 in one line

> **Scarce-pad reservation is distinct from whole-branch-first routing. Q3.6
> current copper is reserved early; U18.8/U18.9 Kelvin endpoints reserve only
> their neck/via exits before controls; U18.2 may start at its already
> authorized measured 0.20 mm rung.**

002M–002S permuted whole-branch order five times and each permutation only chose
a different casualty. A reservation does not re-run that race: it lays the
**minimum** copper that must leave a scarce pad, and leaves the long run for
later, over copper that is no longer scarce.

## 2. Section 3 — the four scarce sites, measured before any signal copper

| pad | net | B.Cu escape | F.Cu | nearest 0.35/0.20 through via |
|---|---|---|---|---|
| `Q3.6` | BAT_SENSE | **1.50 mm, 3 dir** | none — bottom-side SMD | B→In2 @ 0.890 mm (2.275, 54.325) |
| `U18.9` | BAT_SENSE | 0.25 mm, 2 dir | none | B→In2 @ 1.325 mm (4.250, 71.625) |
| `U18.2` | LTC_UV | 0.25 mm, 2 dir | none | B→In2 @ 1.375 mm (4.250, 75.875) |
| `R75.1` | BAT_SENSE | **1.50 mm, 5 dir** | none | B→In2 @ 1.212 mm (5.900, 63.500) |
| `U18.8` | BAT_PROTECTED_P | 0.25 mm, 2 dir | none | B→In2 @ 1.325 mm (3.750, 71.625) |
| `R75.2` | BAT_PROTECTED_P | **1.50 mm, 3 dir** | none | B→In2 @ 1.212 mm (2.400, 63.500) |

**On a clean board not one of these pads is scarce.** Every scarcity 002M–002S
reported was manufactured by copper laid earlier — which is precisely the premise
D-266 rests on, and it is now measured rather than inferred.

## 3. Section 5 — the BAT_SENSE current path, first and real

- `Q3.5 → Q3.6` — **3.770 mm, 1.00 mm, B.Cu, zero vias**
- `Q3.6 → R75.1` — **13.532 mm, 1.00 mm, B.Cu, zero vias**

Both at the 1.00 mm target, no ladder fall, current-path role, outer 1 oz.
`Q3.6` — the pad 002S found walled in behind 28 track segments — routes in one
second when it is asked first.

## 4. Sections 6–8 — the Kelvin exits reserved

| branch | corridor | vias (In2) | outer stubs | via choice |
|---|---|---|---|---|
| `R75.1 ↔ U18.9` | `BAT_SENSE_KELVIN` | U18.9 (4.250, 69.000) · R75.1 (5.650, 64.950) | 3.359 mm | shortest exit |
| `R75.2 ↔ U18.8` | `BAT_PROT_TAP_U18` | U18.8 (3.750, 71.600) · R75.2 (1.200, 65.700) | 3.500 mm | nearest exit, after retry |

Each reservation is a 0.20 mm neck plus one **ordinary 0.35/0.20 through via** —
no microvia, no blind, no buried, no POFV. Each is judged by an **inverted gate**:
DRC gains no class and **the ratsnest must NOT move**, because a reservation that
moves it has connected something, which is the alternate-path failure section 8
forbids.

## 5. Section 9 — LTC_UV

Started at its authorised 0.20 mm rung (option A), returned `NO_PATH` at 0.20 mm
and again at 0.15 mm, and then took **option B**: an ordinary 0.35/0.20 through
via onto In2 and back. `LTC_UV` is **one component**. No global ladder change, no
clearance relaxation, and every other SIG branch keeps LAD_SIG untouched.

## 6. Section 10 — the scoping regression

`d266_probe.py`, 13 checks, **D-266 PROBE: PASS** — the 0.20 mm start names
exactly one branch; every override rung is already in LAD_SIG and none is below
its floor; no netclass or clearance value is touched; D-264 is unchanged; a
failed reservation leaves **zero** copper; and a reservation is counted,
journalled and gated as a reservation, never as a route.

## 7. Sections 12–13 — the control gate: **PASS**

    PR-40 PROBE  targets 111111111  U18 8/8  U19 0/7
                 open U18 pins: none

`U18.1` VIN · `U18.2` LTC_UV · `U18.3` LTC_OV · `U18.6` LTC_SHDN ·
`U18.7` FAULT_N · `U18.10` LTC_GATE · **`U18.8` and `U18.9` Kelvin, connected,
not merely reserved.** `LTC_GATE` all six functional pads · `LTC_SHDN` one
component · `LTC4368_FAULT_N` all four (`U18.7`, `R81.2`, `R82.1`, `Q9.1`) ·
`LTC_OV` one component **on B.Cu with zero vias** · `LTC_UV` one component ·
`Q3_CS` one component through the Q3.3 POFV · `Q2_CS` · `BAT_MID` one component.

**One serious control schedule was needed, not six.** With the scarce exits
already reserved, 002S's winning order `6, 10, 7, 1, 3, 2` closed the field
first time — the schedule sweep 002S needed was a symptom of the race D-266
removes.

## 8. Sections 14–15 — the paired inner Kelvin: **one branch over by 0.456 mm**

Both branches on **In2.Cu**, paired, no F.Cu fallback, no POFV.

| branch | outer stubs | inner run | vias | total | cap |
|---|---|---|---|---|---|
| A `R75.1 → U18.9` | 3.359 mm | 4.630 mm | 2 | **7.989 mm** | ≤10.000 ✓ |
| B `R75.2 → U18.8` | 3.500 mm | 6.956 mm | 2 | **10.456 mm** | ≤10.000 ✗ |

**Mismatch 2.467 mm** against a 5.000 mm limit — **inside spec**. Branch B
exceeds the length cap by **0.456 mm** and that is reported as a failure, not
rounded away. The shortest-exit attempt for branch B *was* tried and was rejected
on `BAT_MAIN routed clearance`; the accepted route is the nearest legal exit.

## 9. Sections 16–17 — the two 002S gate classes

**§16 `BAT_RAW R77.1 → R79.1`: `holes_co_located` is GONE.** It does not recur
anywhere in this task. What stands in its place is different and clearer:
`R79.1 → R80.1` returns `NO_VIA_SITE — no via site of 0.80 mm reachable on B`,
and `R77.1 → R79.1` is rejected on `BAT_MAIN routed clearance`. **Root cause: a
ruled microamp divider tap is being asked for a wide-net via.** `R79.1` is a
D-249 0.20 mm TAP everywhere else in the plan, and the PR-43 bridge row hands it
the `BAT_MAIN` ladder and an 0.80 mm via. Section 16's own preferred resolution —
a local B.Cu TAP with zero vias — is exactly right, and it is a plan-row ladder
change that alters `BAT_RAW` behaviour board-wide, so it is **raised, not taken**.

**§17 `BAT_PROT_SHDN_CTL Q4.1 → R83.1`: CLOSED.** Routed **41.814 mm at 0.15 mm
on F.Cu with 2 ordinary vias**. No new clearance exception was created and none
was needed. The length is honest and worth a look on its own terms — 41.8 mm for
a shutdown control line is long — but it is legal copper under standing rules.

## 10. Sections 19–21 — the trunk, and the whole of the remaining failure

    BAT_PROTECTED_P  R75.2 -> D9.1   NO_LEGAL_ESCAPE
    D9.1: NO LEGAL ESCAPE at >= 1.200 mm; blocked by track (x37), R77.2 (x8), TP17.1 (x5)

**Zero trunk corridor families were generated, and honestly so:** the failure is
at `D9.1`'s escape, before any corridor question can be asked. Scheduled last —
which is what D-263 ruled and what protects the pin field — `D9.1` is behind 37
track segments by the time the trunk is offered its lane. `R75.2` itself is fine:
it escapes at 0.25 mm with five directions and carries the Kelvin reservation.

`BAT_PROTECTED_P` therefore stands at three islands —
`{C25.1, C36.1, R75.2, TP15.1, U11.2, U14.2, U14.3, U18.8}`, `{D9.1}`, `{C58.1}` —
with the `U11.2` flare intact.

## 11. Section 22 — decisive local gate

**FAIL, and the list of what fails is short:** the trunk `R75.2 → D9.1`;
`BAT_PROTECTED_P` complete; `BAT_RAW` complete (`R79.1` split); and Kelvin branch
B by 0.456 mm. **Everything else in section 22 passes** — six-layer architecture,
both GND planes, the frozen fingerprint, Q3.3 POFV with `Q3_CS` and `Q3_GATE`,
U18 8/8 including both Kelvin pins, all six control nets whole, the BAT_SENSE
current path complete on outer 1 oz with zero vias, the Kelvin pair paired on In2
with mismatch inside spec, `R80.1` and `D12.1` on the main `BAT_RAW` island, the
`U11.2` flare, `BAT_PROT_SHDN_CTL` complete, and **no `holes_co_located`**.

## 12. Section 23 — the Q3 placement gate is NOT triggered

Section 23 fires only if `Q3.6` routes early **but cannot coexist** with the
completed controls, Kelvin and trunk. It routes early *and it coexists*:
`BAT_SENSE` carries `Q3.5`, `Q3.6`, `R75.1` and `U18.9` in **one functional
island** after the whole control field and both Kelvin branches are on the board.
**No Q3 displacement is required and none is proposed.** Q3 was not moved.

## 13. Decision required

1. **Rule on the trunk's exit at `D9.1`.** The trunk is the last thing routed and
   `D9.1` is sealed by the time it runs, while routing the trunk earlier is what
   cost three control pins in 002Q. D-266's own answer generalises: **reserve
   `D9.1`'s trunk exit early** — outer, 1.20 mm, zero vias, minimum neck — and
   complete the run afterwards. That extends reservation from sense copper to a
   current path and is a protection-architecture call, so it is raised here.
2. **`BAT_RAW R79.1`**: give the PR-43 bridge rows the TAP ladder the same pads
   already carry everywhere else, per section 16's preferred resolution.
3. **Kelvin branch B, 0.456 mm.** The shortest exit exists and is rejected on
   `BAT_MAIN routed clearance`; a bounded corridor for that one branch, or a
   0.456 mm allowance, is a CTO choice and neither was taken.

## 14. What was NOT done

D9, U18, R76..R83, R75 and Q3 frozen and fingerprint-asserted on every screen; no
placement searched. No placement ECO, no Q3 POFV, no signal track and no signal
via written to the authoritative board — it remains six copper layers, zero
signal tracks, zero signal vias. D-264 untouched. Kelvin limits, `BAT_MAIN`
clearance, the shunt part and the six-layer architecture untouched.
**U19 NOT SEARCHED. Phase A NOT run. Phase B NOT run. Converter routing NOT
started.** B-34 remains open.

---

## 15. Final board, after save / reload

    tracks 219   vias 26
    DRC {'hole_clearance': 5, 'lib_footprint_issues': 199,
         'solder_mask_bridge': 1, 'unconnected_items': 499}

**Byte-for-byte the scratch DRC baseline: zero new classes.** No `via_dangling`
— every reserved via was joined, so the reservation mechanism leaves nothing
behind. No `holes_co_located`. No `shorting_items`. All six standing suites PASS:
`router_regression` (G1–G9), `p1_regression`, `dru_probe`, `netclass_probe`,
`d264_probe`, `d266_probe`.

Per-net truth on that board:

| net | islands | note |
|---|---|---|
| `BAT_SENSE` | 2 | `{Q3.5, Q3.6, R75.1, U18.9}` + `{TP20.1}` — **all four functional pads whole** |
| `LTC_SHDN`, `LTC4368_FAULT_N`, `LTC_OV`, `LTC_UV`, `Q3_CS`, `Q2_CS`, `BAT_MID` | 1 | complete |
| `LTC_GATE` | 2 | all six functional pads whole; `{TP17.1}` alone |
| `BAT_PROT_SHDN_CTL` | 2 | `{Q4.1, R83.1}` whole; `{TP19.1}` alone |
| `BAT_PROTECTED_P` | 3 | `{…, R75.2, U18.8, U11.2, U14.2, U14.3}` + `{D9.1}` + `{C58.1}` — **the trunk** |
| `BAT_RAW` | 3 | main island carries `U18.1`, `R80.1`, `D12.1`; `{R79.1}` split |
