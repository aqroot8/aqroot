# FBV2-P2-002I — transcript

**Date:** 2026-08-26 · **Starting HEAD:** `33bbd24` · **Result:** FAIL (§5 Case D)

## What was run, in order

1. **Preflight.** HEAD = origin/master = `33bbd24`, tracked tree clean, no stale
   processes. Authoritative PCB: 0 signal tracks, 0 signal vias, In1.Cu a single
   `GND` zone. `p1_regression` PASS, `router_regression` PASS (34 checks,
   G8-A…G8-F included), `dru_probe` PASS, `netclass_probe` PASS,
   `fork_equivalence` PASS. DRC baseline 1 `solder_mask_bridge`, 499 unconnected,
   parity 0. ERC 0 severity-error.

2. **A bare-board negative test at the 002F-validated `R80` pose** — the previous
   corridor measurement had been taken at C00's pose, not this one. It did not
   finish inside its budget and was abandoned rather than retried; the real
   experiment answers the same question better. `R80.1` sits at (5.500, 71.325)
   in this placement, ~24.8 mm from `Q2.7`.

3. **ONE complete Phase A**, PR-39 + PR-41 + PR-43, clean scratch, judged on the
   saved-and-reloaded ledger. One long router job at a time throughout.

4. **Diagnosis of the two casualties** — copper within 1.2 mm of `U18.7` and
   `U18.10`, by net, layer and width.

5. **§9 regression measurement** — U18 pin-by-pin connectivity, Kelvin lengths
   and mismatch, VIN, `Q3_CS`/`Q2_CS` via counts, `BAT_PROTECTED_P` width and
   vias, `U11.2` neck, `U14`/`TP15`, test points.

6. **PR-43 put behind `AQROOT_PR43`**, default OFF. `router_regression` re-run:
   ALL CHECKS PASS.

## The result in one line

PR-43 closes `BAT_RAW` and `LTC_SHDN` with no placement change, and costs `U18`
two pins. Both orderings score 24 of 29.

## The measurement that mattered

No `BAT_RAW` copper lies near either stranded pad. `LTC_SHDN` and
`BAT_PROTECTED_P` sit 0.500 mm from `U18.7`; `BAT_SENSE` sits 0.500 mm from
`U18.10`. PR-43 unblocked `LTC_SHDN`, whose new route took `U18.7`'s lane — so
the west margin is oversubscribed and reordering only moves the casualty.

## What was deliberately not done

- **No U19 search.** §6 requires Case C; this is Case D.
- **No R80/R81 sweep.** §2 forbids it before PR-43 is tested, and PR-43 proved
  R80 is not the root cause of `BAT_RAW`.
- **No authoritative write.** §12 requires Phase A and Phase B PASS.
- **No parallel router jobs**, and no polling of a running route.

## Judgement calls

- **PR-43 flagged rather than adopted or reverted.** Adopting it trades two §9-
  protected U18 pins; reverting it discards a measured result. The flag keeps
  both and leaves the trade to the CTO, which is what Case D asks for.
- The abandoned negative test in step 2 was not retried. It would have told us
  something the Phase A tells us better, and §-process says one long job at a
  time.
