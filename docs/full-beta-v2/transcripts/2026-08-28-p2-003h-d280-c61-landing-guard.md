# FBV2-P2-003H — transcript: the 003G functional casualty `N_BATDIV C61.1→U19.6` is a co-located through-via landing; an env-gated hole-to-hole guard in `connect_hop` rejects the co-locating site, falls the placement through to `via_site`'s legal 0.45 mm barrel, and closes C61.1 on the full production run net-positively — D-277/D-278/D-279 and the proven 003C bridge held fixed; the terminal item is the optional TP20 TEST point (not a regression)

**Date:** 2026-08-28 · **Starting HEAD:** `8e1c716` (verified clean, `HEAD ==
origin/master` at start). Full narrative in
[`audits/2026-08-28-p2-003h-d280-c61-landing-guard.md`](../audits/2026-08-28-p2-003h-d280-c61-landing-guard.md).

This transcript records the D-280 CLOSEOUT: validation of the parent-supervised
definitive full run (`phaseA_003h_fix.json`, scratch FIX003H3, session 62295), an
audit of the guard, completion and running of the standing probe, the exact
casualty ledger vs 003G, and the honest disclosure of the optional TP20 test
point. No long full route was re-run (the parent CTO supervised it to completion).

## What was done, in order

1. **Verified start state.** `HEAD == origin/master == 8e1c716`. Dirty worktree:
   `qrouter.py` (the D-280 guard) and the per-run scratch `phaseA_journal.json`
   modified; untracked `phaseA_003h_fix.json`, `u19_escape_probe_003h.py`, and an
   incomplete `log_FIX003H2.txt`.

2. **Validated the definitive result and the D-280 landing.** `phaseA_003h_fix.json`:
   fail `BAT_SENSE TP20.1→(node) (TEST) NO_PATH` (optional), connections 71,
   skipped 96, ratsnest 708/−73, DRC `{hole_clearance 5, lib_footprint_issues 199,
   solder_mask_bridge 1, unconnected_items 499}` identical to baseline, `bridge_eco
   null`. Independent hole-to-hole scan of all 52 through-via barrels: **zero**
   edges below the 0.25 mm floor. The two co-terminal `N_BATDIV` barrels at U19.6
   land (4.950, 27.950) and (5.400, 27.950): **centre 0.450 mm, hole-edge 0.2500 mm
   — at the governing floor**, no co-location. Confirmed C61.1 and the D-277/D-278/
   D-279 gains all routed.

3. **Confirmed the authoritative PCB is untouched.** No `kicad/` file modified or
   untracked; `aqroot-Beta-v2.kicad_pcb` has 0 copper segments and 0 copper vias;
   `aqroot-Beta-v2.kicad_pro` `min_hole_to_hole = 0.25` unchanged.

4. **Audited the guard.** The D-280 block lives in `qrouter.connect_hop`'s
   `free_everywhere`: after the existing all-layers `point_free` copper check
   (which `point_free` waives for the via's own net), it now enforces the
   net-agnostic `min_hole_to_hole` floor for the barrel about to be drilled,
   rejecting a co-locating site so the placement falls through to `via_site` (which
   already enforces the floor). Env-gated `AQROOT_D280`; `h2h = 0` when unset. The
   `250000` (0.25 mm) equals the governing DRU floor and is byte-identical in
   structure to the existing `via_site(hole_clr=250000)` and POFV
   `max(h.hx,h.hy)+via_drill/2.0+250000` — safely aligned, not a new/looser value.
   The guard only ADDS a rejection; it relaxes nothing and moves nothing.

5. **Completed and ran the standing probe `u19_escape_probe_003h.py` (A/B/C/D/E).**
   PASS: A geometry fine + BASE two-barrel landing at the floor (c2c 0.450 /
   edge 0.2500); B/C deterministic reproduction — D-280 OFF lands C61.1 co-located
   (edge −0.1500 at (4.900,27.950)), ON legal (edge +2.0000 at (2.750,27.950)),
   env-gated; D the guard present/hole-to-hole/stricter-only; E full-run — the
   003G fix (D-280 off) does NOT route C61.1, the 003H fix (D-280 on) ROUTES it,
   net-positive (69→71), DRC histogram identical, both D-279 named gains preserved,
   `bridge_eco null`.

6. **Ran the regression fleet (no long route).** `u19_escape_probe_003g` (D-279),
   `u19_escape_probe_003f` (D-278), `u19_escape_probe_003e` (D-277) PASS;
   `router_regression` ALL CHECKS G1–G11 PASS (D-280 off → pre-003H behaviour);
   `bridge_probe_003c` PASS (003C/D-275 held fixed); `bridge_probe_003d` PASS.

7. **Built the exact casualty ledger vs 003G.** Connections 69 → 71 (+2):
   + `N_BATDIV C61.1→U19.6` (the D-280 target, restored), + `BAT_RAW R77.1→R79.1`,
   + `LTC4368_FAULT_N R82.1→Q9.1` with − `LTC4368_FAULT_N Q9.1→(node)` (the same
   net, Q9.1 re-landed on its real pad). Functional C61.1 restored; D-277/D-278/
   D-279 gains retained; the terminal `BAT_SENSE TP20.1→(node)` is a TEST-role
   test point unrouted since 003F and terminal in 003G too — NOT a functional
   regression, only its fail reason shifted (clearance → NO_PATH). Disclosed.

8. **Cleaned up and closed out.** Restored `phaseA_journal.json` scratch to HEAD;
   trashed the incomplete `log_FIX003H2.txt`; kept the definitive `phaseA_003h_fix.json`
   and the probe. Wrote the audit, CTO_DECISIONS D-280 row, CHANGELOG, PROGRESS,
   and this transcript. Defined FBV2-P2-003I (integrate the held-fixed 003C bridge
   into the production run; treat optional TP20 separately).

## What was NOT done

- No long full production route was re-run (the parent CTO supervised it to
  completion in session 62295). No authoritative copper, no placement ECO, no part
  moved, no topology/net/footprint/polarity/safety change, no six-layer/GND change,
  no netclass/width/clearance/hole-to-hole relaxation (the 0.25 mm floor is
  ENFORCED). `c3_00` NOT promoted. `bridge_probe_003c/003d` behaviour preserved.
  Phase A NOT passed (the held-fixed 003C BPP bridge is not yet integrated into the
  production run); Phase B NOT run. `/home/aqroot8/.aqroot-progress.env` untouched —
  the CTO performs the readiness review after accepting this pushed milestone. No
  OWNER decision exists or was made — 003I is engineering scope within CTO authority.

## Artifacts committed

- `qrouter.py` — the env-gated D-280 net-agnostic hole-to-hole guard in
  `connect_hop`'s `free_everywhere`.
- `u19_escape_probe_003h.py` — the D-280 standing probe (A/B/C/D/E).
- `phaseA_003h_fix.json` — the definitive full run (D-279 on, D-280 on; C61.1
  closed; scratch FIX003H3).
- Audit, CTO_DECISIONS D-280 row, CHANGELOG, PROGRESS, this transcript.
