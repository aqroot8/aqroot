# FBV2-P2-003E — transcript: the U19.3 escape is a route-order contention, fixed by a cardinality-0 planar tie-break

**Date:** 2026-08-28 · **Starting HEAD:** `f3ba82a` (verified: `HEAD ==
origin/master`; worktree carried only the uncommitted 003E artifacts — the
`order_tight` planar tie-break in `route_battery_block.py`,
`u19_escape_probe_003e.py`, the draft audit — plus the `phaseA_journal.json`
scratch churn). Full narrative in
[`audits/2026-08-28-p2-003e-d277-u19-escape-order.md`](../audits/2026-08-28-p2-003e-d277-u19-escape-order.md).

This transcript records the D-277 closeout, resumed from a dirty worktree after
the prior task returned prematurely (no commit, placeholder docs) while its
definitive full-driver run was still active.

## What was done, in order

1. **Recovered the live run instead of relaunching it.** A prior full-driver
   route was still running as PID 288971 (parent `systemd --user`, detached from
   the ended turn). It was verified **healthy**, not stalled: state `Rl`, main
   thread ~92 % CPU, ~500 CPU jiffies / 5 s (one core pegged), `wchar` and log
   advancing across a long single-net route (individual routes ran 30–182 s,
   explaining quiet log stretches). Its real stdout/stderr was `/tmp/log_full_e7.txt`
   (fd 1/2) and its result artifact `phaseA_003e_fix.json` (from
   `AQROOT_RESULT`) — the prior report's `/tmp/log_u19_fullfix.txt` path was
   wrong. No duplicate run was launched.

2. **Waited synchronously for it to exit.** Polled `kill -0 288971` in
   foreground until exit — no nohup, no disown, no detached waiter, no early
   return. The run finished at 06:44 UTC after **2306.8 s (~38 min)**, writing
   `phaseA_003e_fix.json` (48 KB).

3. **Audited the fix before trusting the run.** The uncommitted change is a
   scoped planar tie-break inside `order_tight`: it carries each tied pin's tight
   pad and its target, and among rows tied on `(slack, ways-out)` with
   `ways-out ≤ 1` counts tied siblings whose pad falls inside the pad→target
   bounding span, appending `crossings` as the LAST sort key. Confirmed
   `pads.get(net,{}).get(ref)` and `x`/`y` access match existing `order_tight`
   usage; the term is a no-op for any pin with a second way out (`fr_a ≤ 1` guard)
   and cannot reorder across tightness classes. Correct and tightly scoped.

4. **Consumed the definitive result.** The pin-field slack line reordered
   `U19.3 +0.14/1way  U19.2 +0.14/1way …` (U19.3 ahead of U19.2, exactly the
   tie-break's prediction) and **both boxed pins routed** — `N_POL TP23.1→U19.3`
   6.118 mm OK, `REF_POL TP24.1→U19.2` 13.372 mm OK. The D-276 blocker is
   **cleared**; Phase A advances to a new terminal blocker
   `VREC_VCC U19.8→(node) NO_LEGAL_ESCAPE at ≥0.150 mm` (blocked by track ×33,
   U19.7 ×13, board_edge ×5, U19.5 ×5), co-terminal with `VBRIDGE_TOP R85.1`;
   both sit at −0.15/0way, stable across all 3 passes. Aggregate is flat by
   design (the fix relocates the binding blocker): connections 68, skipped 90,
   ratsnest 711 Δ −70 (003D-base 68/91/710/−71); DRC identical to baseline;
   `bridge_eco null`. Phase A **did not complete**.

5. **Ran the focused probe and the standing suites.**
   `u19_escape_probe_003e.py` **PASS** (A refuted, B both directions, C planar
   prediction + lay-and-count lookahead agree, D driver carries the scoped
   tie-break); `router_regression.py` **ALL CHECKS PASS** (G1–G11);
   **`bridge_probe_003c.py` PASS** (the proven 003C vacate + F.Cu bridge held
   fixed); **`bridge_probe_003d.py` PASS** (the committed 003D FAIL artifacts
   still pin `N_POL U19.3`, un-regressed).

6. **Restored the scratch.** `phaseA_journal.json` — a per-run scratch the
   driver overwrites (`json.dump(…, 'phaseA_journal.json')`), last committed in
   002W and untouched by every milestone since — reset to HEAD, not committed.

7. **Completed the docs and committed.** Filled every audit placeholder
   (full-run outcome, next blocker, suites); added the D-277 CTO_DECISIONS row,
   CHANGELOG and PROGRESS entries, and this transcript. Committed the driver
   change, the probe, the result `phaseA_003e_fix.json`, and the docs.

## The verdict

D-277 is a **measured, proven route-order fix.** The D-276 `U19.3 N_POL`
NO_LEGAL_ESCAPE was cause **(B)** — a route-order contention in U19's west row,
not (A) intrinsic pad/board geometry (refuted: the pad escapes on the empty
board) and not (C) placement (not required). A **cardinality-0** planar
tie-break clears it on a full production run and Phase A advances one net deeper
to `VREC_VCC U19.8` — a **different class** of blocker (foreign laid track on the
east row), for which the west-row tie-break does not apply. Not an owner
decision.

## State

Authoritative PCB UNCHANGED — six copper layers, 0 signal tracks, 0 signal vias,
no KiCad source mutated (the full run applies no ECO and lays no authoritative
copper; final DRC = baseline). All suites PASS and unregressed:
`u19_escape_probe_003e`, `router_regression` G1–G11, `bridge_probe_003c`,
`bridge_probe_003d`. `c3_00` NOT promoted; D-249…D-276 (incl. **D-275**)
untouched; no safety weakening, no topology/net/footprint/polarity change, no
authoritative promotion (Phase A did not pass). `/home/aqroot8/.aqroot-progress.env`
unchanged. **U19.3 cleared; `VREC_VCC U19.8` is the new named blocker;** Phase A
NOT completed; Phase B NOT run. B-34 unchanged. **No progress earned: PCB routing
stays 0 %, overall stays 74 %.** Next: **FBV2-P2-003F** — discriminate the
`VREC_VCC U19.8` (and co-terminal `VBRIDGE_TOP R85.1`) NO_LEGAL_ESCAPE the same
way 003E discriminated U19.3, without assuming another cardinality-0 fix exists,
holding the proven 003C bridge fixed.
