# FBV2-P2-002J — transcript

**Date:** 2026-08-26 · **Starting HEAD:** `bdafb28` · **Result:** FAIL

## Order of work

1. **Preflight** — HEAD = origin/master = `bdafb28`, tree clean, no stale
   processes, board 0/0 tracks/vias, In1.Cu one `GND` zone. `p1_regression`,
   `dru_probe`, `netclass_probe`, `fork_equivalence`, `router_regression` all PASS.

2. **Built the §5 local screen** (`AQROOT_LOCAL=R80`) plus an ECO override hook
   (`AQROOT_ECO_EXTRA`) so a candidate pose is tested without editing the
   committed ECO.

3. **Validated the screen against the known answer before trusting it.** The
   control reproduces D-255 exactly — U18 6/8, open `U18.7`/`U18.10`, `R80.1`
   and `D12.1` connected — in 471 s rather than ~2 h.

4. **Screened six R80 poses**, one heavy job at a time, in the foreground.

5. **Two full Phase A runs**, K6 then K1, background, one at a time.

6. **Fixed PR-44** when the first two full runs died at the same connection.

7. **Stopped** on §10's stop condition and wrote up.

## Things that went wrong, and what they cost

- **Two jobs wrote the same log file.** An earlier background run and a
  foreground run both redirected to `K0.log`, and I read the mixture as one
  result — it showed U18 0/8 and a queue full of closure entries that the
  foreground run had gated off. Every probe after that got a unique filename.
- **Background jobs do not survive a session resume.** Three were killed
  mid-flight. The screen was made cheap enough to run in the foreground partly
  for this reason.
- **PR-44 was mistaken for flakiness first.** Two SIGSEGVs looked intermittent
  until the fault frame showed the *same* connection number both times. Reading
  the frame instead of retrying is what turned it into a five-minute fix.

## The finding

Moving `R80`/`R81` closes the two pins D-255 named and breaks other things
worth more. `Q3_CS` splitting under K6 is the clearest single reason to stop:
§12 protects it, and no reordering or repositioning tried so far has beaten
24 of 29.

## Deliberately not done

- **No U19 search** — §9 requires an R80/R81 winner first, and there is none.
- **No FAULT_N via** — §10 authorises it only if `U18.7` is the sole blocker,
  and `U18.7` turned out to be the easiest pin of the set.
- **No via on `LTC_GATE`** — forbidden by §10, which instead orders the stop
  this task ends on.
- **No authoritative write, no Phase B, no manifest.**
