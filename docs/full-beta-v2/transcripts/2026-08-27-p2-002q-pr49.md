# FBV2-P2-002Q — PR-49 routing closeout

**Date:** 2026-08-27 · **Starting HEAD:** `adabe98` · **Result: FAIL** at §14

**Authoritative stackup NOT changed** (§18 gates it on §14). PCB byte-identical
to `adabe98`, md5 `a908cedfa9f9410aab327d8bd55b9f45`, **4 copper layers,
0 signal tracks, 0 signal vias**. All five suites PASS. **Routing 0 %,
overall 74 %.**

Full audit: [`audits/2026-08-27-p2-002q-pr49.md`](../audits/2026-08-27-p2-002q-pr49.md)

---

## PR-49 is delivered and it works

```
BAT_PROTECTED_P R75.2 -> D9.1
  1.50 mm  routed, then rejected: copper_edge_clearance
  LADDER_RETRY  1.50 mm rejected, falling to 1.20 mm
  1.20 mm  19.219 mm, B.Cu, zero vias  -- ACCEPTED
```

and a second instance appeared unprompted — `LTC_GATE U18.10 → Q3.4`, 0.20 mm
rejected, falling to 0.15 mm. **`BAT_PROTECTED_P` now carries `D9.1` in its
functional island**, closing the split 002P reported.

The rule lives in one place, `route_battery_block.ladder_retry`, so it is
regression-tested directly rather than by reading. **G9** in
`router_regression.py` pins all five properties §5 asked for — falls to the next
authorised rung, never invents a rung below the ladder, fails cleanly when every
rung is rejected, does *not* walk the ladder on a non-gate failure, and leaves
no copper behind (0 → 5 → 0 tracks). `router_regression`: ALL CHECKS PASS.

**The safety boundary is structural, not a promise:** the retry only ever walks
the ladder the path role already had. `PLAN_1_BPP_TRUNK` remains exactly
1.50 mm target / 1.20 mm floor.

## And closing the trunk costs three control pins

On the identical frozen placement, 002P reported **U18 8/8 with the trunk
absent**. With PR-49 laying 19.219 mm of 1.20 mm trunk, U18 falls to **5/8** —
`U18.2`, `U18.3`, `U18.7` open. The two results do not conflict; they are the
same board with one more connection on it. **002P's 8/8 was in part an artefact
of a connection that never got laid.**

## The Kelvin ordering hypothesis is measured FALSE

§9 proposed routing the Kelvin taps early and required it measured, not assumed.

| screen | ordering | U18 | routed Kelvin |
|---|---|---|---|
| R1 | Kelvin taps first | **4/8** | — |
| R2 | plan order | **5/8** | 8.667 / **11.130** mm |

The same-net argument is correct and beside the point: the taps do not obstruct
the *trunk*, they take the pin-field lanes `LTC_UV`, `LTC_OV`, `LTC_SHDN` and
`FAULT_N` need. The flag stays in the harness, off, so a rejected hypothesis
stays reproducible instead of becoming folklore.

Best routed Kelvin: mismatch **2.463 mm** (inside 5.000), but
**`R75.2 → U18.8` is 11.130 mm against a 10.000 mm cap** and `R75.1 → U18.9`
takes an F.Cu excursion where §8 prefers local B.Cu. §11's corridor topologies
were never reached — ordering, the cheaper instrument, made things worse.

## §14 gate

**PASS:** stack, In1/In4 planes, D9 legal, `R75.2 → D9.1` at 1.20 mm,
`BAT_PROTECTED_P` one functional island, `U11.2` flare, `BAT_SENSE` one island
(1.00 mm B.Cu, 0 vias), `Q3_CS`, `Q3_GATE`, Q3.3 POFV, `LTC_SHDN`, `BAT_RAW`
with `U18.1`/`R80.1`/`D12`, `BAT_MID`, `Q2_CS`.

**FAIL:** Kelvin `R75.2 → U18.8` 11.130 mm · U18 5/8 · `LTC_GATE` split
`[Q2.2, Q2.4, Q3.2, Q3.4]` / `[R76.1, U18.10]` · `LTC_OV` three islands ·
`FAULT_N` `[Q9.1, R81.2, R82.1]` / `[U18.7]`.

§15's sole-blocker clause does not apply — FAULT_N is one of four, and the cause
is shared: the trunk copper now occupies the pin-field lanes.

## The decisions

1. **The trunk-versus-pin-field contention needs a ruling** — routing order,
   a bounded trunk corridor, or a different trunk path. None is this task's to
   choose.
2. **`R75.2 → U18.8` is 1.130 mm over the Kelvin cap**; §11's corridor
   topologies B and C remain unspent and are the next instrument.
3. **PR-49 is settled** and needs no further decision.

**U19 NOT searched (§20). Phase A NOT run. No battery signal copper. Converter
routing NOT started. B-34 OPEN.**
