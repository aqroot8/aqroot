# FBV2-P2-003F — transcript: the U19.8 escape is a crossing-pin B.Cu detour, fixed by a layer hop; R85.1 is a distinct blocker deferred to 003G

**Date:** 2026-08-28 · **Starting HEAD:** `ef94e67` (verified clean, `HEAD ==
origin/master` at start). Full narrative in
[`audits/2026-08-28-p2-003f-d278-u19-8-crossing-hop.md`](../audits/2026-08-28-p2-003f-d278-u19-8-crossing-hop.md).

This transcript records the D-278 closeout: a bounded A→B→C investigation of the
D-277 next blocker `VREC_VCC U19.8` and its co-terminal `VBRIDGE_TOP R85.1`, a
measured minimum repair of U19.8, and the honest deferral of R85.1 to 003G.

## What was done, in order

1. **Verified start state.** `HEAD == origin/master == ef94e67`, clean worktree.
   Read the source-of-truth docs (D-275/D-276/D-277 rows, the 003E audit and
   result `phaseA_003e_fix.json`) and the driver (`route_battery_block.py`
   `order_tight` / `run_once`, `qrouter.py` `escape`/`connect_role`/
   `connect_hop`, `battery_route_plan.py` dead-cell plan).

2. **Cause (A) refuted — cheapest probe first, no full run.** On the empty
   authoritative board `qb.escape(...)` gives U19.8 = 5 ways, R85.1 = 8, D10.1 =
   7 at ≥ 0.150 mm. Intrinsic east-row geometry is not the blocker.

3. **Cause (B) confirmed and localised with a cheap real-router reproduction.**
   Added a bounded `AQROOT_LOCAL=DEADCELL` prefix (routes only stages 10a/10b,
   skips the west-margin prefix — the same class as R80/D256/U19). It reproduced
   the blocker byte-similar (`U19.8 … blocked by track ×34, U19.7 ×14` vs the
   full run's `×33/×13`), proving the contention is entirely inside the dead-cell
   block; the west-margin prefix (y 60–93) never reaches U19's y ≈ 29 corridor.

4. **Attributed the aggressor on the real routed board.** Only two edges route
   before U19.8: `N_POL TP23.1→U19.3` (6.118 mm) and `REF_POL TP24.1→U19.2`
   (13.372 mm). Per-segment attribution: U19.2 — the D-277 *crossing pin*, routed
   last, its direct southern lane filled by U19.3's copper — takes a **northern
   horseshoe** laying a wall `(4.65,29.85)→(3.05,29.85)` at d = 0.495 mm north of
   U19.8 and a diagonal at d = 0.901 mm, sealing it to 0 B.Cu escape (so it
   cannot even start the F.Cu-hop fallback). The same detour also threatens
   R85.1. The D-277 planar span tie-break cannot catch it (U19.2's straight-line
   span excludes U19.8; U19.8 is a multi-lane victim, not the tied single-lane
   class).

5. **Proved the fix with the real router.** Routing the crossing pin U19.2 via an
   ordinary 0.35/0.20 through-via F.Cu hop (8.610 mm, direct) instead of the B.Cu
   horseshoe leaves U19.8 at 5 ways and it routes; R85.1 stays escapable through
   the U19.8 fix.

6. **Implemented the minimum repair, reusing the D-277 predicate.** `order_tight`
   records the crossing pin (`crossings > 0`, guarded `fr_a ≤ 1`) in
   `hop_first_keys`; `run_once` tries the F.Cu hop first for those keys and falls
   through to the ordinary B.Cu ladder untouched on failure. Scoped exactly to
   the D-277 class — inert for any pin with a second way out; changes only which
   layer the already-identified crossing pin runs on; lays no authoritative
   copper.

7. **Ran the full integrated production validation in the foreground (owned by
   this task).** Recipe = the pinned D-271 production recipe (SIXLAYER, c3_00
   asserted, D256=GSQ, Q3_POFV, D266, D267=F1, TRUNK_LAST, U18_ORDER=6,10,7,1,3,2,
   LOCAL empty, no BRIDGE_ECO) — the same base config as `phaseA_003e_fix.json`,
   confirmed against the committed `log_c3repro003d.txt`. The run was launched as
   a single harness-tracked job, owned and waited on to completion (no
   nohup/setsid/disown, no early return). Result `phaseA_003f_fix.json`, 2330 s:
   `REF_POL TP24.1→U19.2` 8.610 mm F.Cu+2 vias and `VREC_VCC U19.8→R84.2`
   22.408 mm — **U19.8 CLEARED**. Aggregate a wash by design (connections 68,
   skipped 94, ratsnest 710 / −71, one net better than 003E's 711 / −70; DRC
   identical to baseline; `bridge_eco null`).

8. **Classified R85.1 honestly.** R85.1 was never routed in 003E either; on the
   real routed 003F board it is boxed by **N_POL** (×10 within 2.0 mm), a
   different aggressor from the REF_POL crossing, and neither it nor its
   aggressor is single-lane — outside the D-278 scope. Same pattern:
   `REF_HO R92.1↔R93.2` boxed by REC_GATE_N ×8. This is the dead-cell
   resistor-field congestion class → defined as **003G**. The BPP closure
   (`D9.1/C25/C36/C58` at ≥ 1.20 mm) is the KNOWN held-fixed 003C bridge case.

9. **Regression + closeout.** New `u19_escape_probe_003f.py` (A/B/C/D/E, E
   pinning the committed full run routes U19.8 and NOT R85.1) PASS;
   `u19_escape_probe_003e` (D-277 intact) PASS; `router_regression` all checks
   G1–G11 PASS; `bridge_probe_003c` PASS (003C/D-275 held fixed);
   `bridge_probe_003d` PASS (003D FAIL artifacts un-regressed). Restored
   `phaseA_journal.json` scratch to HEAD; scratch `w/` dirs are gitignored.

## What was NOT done

- No authoritative copper, no placement ECO, no part moved, no topology/net/
  footprint/polarity/safety change, no six-layer/GND change, no netclass/width/
  clearance relaxation. `c3_00` NOT promoted. `bridge_probe_003c/003d` behaviour
  preserved. Phase A NOT passed (dead-cell resistor-field + held-fixed BPP bridge
  remain); Phase B NOT run. `/home/aqroot8/.aqroot-progress.env` untouched — CTO
  performs the readiness review after accepting this pushed milestone.

## Artifacts committed

- `route_battery_block.py` — the D-278 crossing-pin layer hop (`hop_first_keys`
  in `order_tight` + `run_once`) and the `AQROOT_LOCAL=DEADCELL` bounded probe
  prefix.
- `u19_escape_probe_003f.py` — the D-278 standing probe.
- `phaseA_003f_fix.json` — the full production run (U19.8 cleared).
- `phaseA_003f_deadcell_base.json` / `phaseA_003f_deadcell_fix.json` — the cheap
  dead-cell-only discriminator (control + fix).
- Audit, CTO_DECISIONS D-278 row, CHANGELOG, PROGRESS, this transcript.
